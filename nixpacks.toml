[providers]
php = "8.4"

[phases.setup]
nixPkgs = ["php84", "nodejs", "npm"]

[phases.install]
cmds = ["composer install --optimize-autoloader --no-scripts --no-interaction --ignore-platform-req=php"]

[phases.build]
cmds = ["npm install", "npm run build", "php artisan migrate --force"]
